<!DOCTYPE html>
<html>
<head>
    <title>Login Page</title>
    <style>
        body {
            background-color: #f0f0f0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
        }
        .container {
            background-color: #cccccc;
            padding: 40px;
            border-radius: 5px;
            font-size: 18px;
        }
        .saml {
            background-color: #008CBA;
            color: white;
            border: none;
            padding: 15px 32px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 4px 2px;
            cursor: pointer;  /* Added pointer cursor on hover */
            transition: background .3s ease-in-out;  /* Added transition effect */
        }
        .saml:hover {
            background-color: #005580;  /* Changed background color on hover for click effect */
        }
        form {
            margin-bottom: 20px;
        }
    </style>
    <script>
        function samlLogin() {
            location.replace("/saml");
        }
    </script>
</head>

<body>
{% if errorMsg %}
    <div class="error_message">{{ errorMsg }}</div>
{% endif %}

<div class="container">
    <form method="post" action="/login">
        <label for="username">Username:</label><br>
        <input type="text" id="username" name="username"><br>
        <label for="password">Password:</label><br>
        <input type="password" id="password" name="password"><br>
        <input type="submit" value="Submit">
    </form>
    <button class="saml" type="button" onclick="samlLogin()">Login with SAML API</button>
</div>
</body>
</html>